/* Description:
We need a function that can transform a number (integer) into a string.
*/

/* 
problem analysis:
-1 We have a number
-2 We need to convert the number to a string
-3 We need to return the string
*/

/* 
steps of solution:
-1 Convert the number to a string
-2 Return the string
*/

function numberToString(num) {
  return `${num}`;
}

console.log(numberToString(5));

// another solution
/* function numberToString(num) {
  return num.toString();
} */
